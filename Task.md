1. **Create the `Book` class** with the fields `title` and `author`. Add the constructor, getters, setters, `equals`, `hashCode`, and **`toString`** methods (mandatory).

2. **Implement all the methods of the `Library` class**, considering all possible cases, such as invalid parameters, `null`, etc.

3. **Create a test class - `LibraryTest`**. Write tests to cover all the main scenarios for the methods of the `Library` class.

4. **Run the tests** and ensure that they pass.
